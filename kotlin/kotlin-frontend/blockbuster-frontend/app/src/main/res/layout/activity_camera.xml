<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 카메라 미리보기 -->
    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="false"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:importantForAccessibility="no" />

    <!-- ✅ 가운데 프레임 (인식 영역 표시) : PreviewView "밖"에, 위에 겹치도록 -->
    <FrameLayout
        android:id="@+id/scanFrame"
        android:layout_width="291dp"
        android:layout_height="298dp"
        android:layout_gravity="center"
        android:background="@drawable/design_grid_640x640_white_on_transparent" />

    <!-- 상단 바 (닫기 / 플래시 / 카메라 전환) -->
    <ImageButton
        android:id="@+id/btnClose"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="top|start"
        android:layout_marginStart="20dp"
        android:layout_marginTop="20dp"
        android:background="@android:color/transparent"
        android:src="@drawable/design_camera_x"
        android:contentDescription="@string/camera_close" />

    <ImageButton
        android:id="@+id/btnFlash"
        android:layout_width="56dp"
        android:layout_height="48dp"
        android:layout_gravity="top|center_horizontal"
        android:layout_marginTop="20dp"
        android:background="@android:color/transparent"
        android:scaleType="center"
        android:src="@drawable/design_camera_flash"
        android:contentDescription="@string/camera_flash_toggle"/>

    <!-- 동그란 촬영 버튼 -->
    <ImageButton
        android:id="@+id/captureButton"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="36dp"
        android:background="@drawable/design_camera_button"
        android:contentDescription="@string/camera_take_picture"/>

</FrameLayout>
